name: CI

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.9", "3.11"]
      fail-fast: false

    steps:
      - uses: actions/checkout@v4
        with:
          # test/weak_field.* are git-lfs objects; without this the checkout holds
          # pointer files and test_load / test_elemental / test_displacement_elemental
          # fail at collection time.
          lfs: true

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install project with dev dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Run CPU tests
        run: pytest -m "not (gpu or mpi or integration)" -v

      - name: Lint with ruff
        run: ruff check lattice/
